import { useViewModel } from '@/features/components/container/quiz/useViewModel'

export const Quiz: React.FC = () => {
  const {} = useViewModel()
  return (
    <main>
      <h1>Quiz</h1>
    </main>
  )
}
